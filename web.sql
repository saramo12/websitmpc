-- CREATE DATABASE website_data;

-- USE website_data;

-- CREATE TABLE viewer_counter (
--     id INT AUTO_INCREMENT PRIMARY KEY,
--     page_name VARCHAR(255) NOT NULL,
--     view_count INT DEFAULT 0
-- );

-- -- Insert an initial record for your homepage
-- INSERT INTO viewer_counter (page_name, view_count) VALUES ('index.html', 0);
